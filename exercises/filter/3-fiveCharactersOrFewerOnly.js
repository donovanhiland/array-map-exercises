/**
 * Given an array of strings, return a new array that only includes those that
 * are 5 characters or fewer in length
 * 
 * Ex.
 * fiveCharactersOrFewerOnly(["dog", "wolf", "by", "family", "eaten", "camping"])
   // ["dog", "wolf", "by", "eaten"]
 */

const fiveCharactersOrFewerOnly = (arr) => {
  // your code here
}

export { fiveCharactersOrFewerOnly }
